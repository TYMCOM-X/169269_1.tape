Fix consecutive page display.
Improve error messages for OPEN and LOOKUP errors.
Put multiple pages per line in one-file mode.
Add bit map for page I/O errors, keep processing when they occur,
  and add trailer report for all I/O errors. (Get disk retrieval
  pointers for all bad pages & display?)
